{
  "name": "meteor",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "cleanup": "rm -rf ../bundle/",
    "deploy": "meteor build ../ --directory --architecture os.linux.x86_64 --server-only;cp app.yaml ../bundle/ && cp Dockerfile ../bundle/;cd ../bundle && gcloud app deploy --verbosity=info -q"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0",
    "apnagent": "^1.1.3",
    "bcrypt": "^3.0.0",
    "coinex.com": "^1.0.2",
    "cryptocompare": "^0.5.0",
    "fibers": "^3.0.0",
    "meteor-babel": "^7.0.0-rc.2",
    "meteor-node-stubs": "^0.3.2",
    "moment": "^2.22.2",
    "node-fetch": "^2.1.2",
    "node-schedule": "^1.3.0",
    "simpl-schema": "^1.5.0"
  }
}
